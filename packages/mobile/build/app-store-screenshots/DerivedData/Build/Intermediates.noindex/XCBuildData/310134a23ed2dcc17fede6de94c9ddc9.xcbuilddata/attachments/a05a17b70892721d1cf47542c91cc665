#!/bin/sh
# Auto-generated by expo-modules-autolinking
CONFIG="release"
if echo "$GCC_PREPROCESSOR_DEFINITIONS" | grep -q "DEBUG=1"; then
  CONFIG="debug"
fi
LAST_CONFIG_FILE="$PODS_ROOT/ExpoModulesWorklets/artifacts/.last_build_configuration"
if [ -f "$LAST_CONFIG_FILE" ] && [ "$(cat "$LAST_CONFIG_FILE")" = "$CONFIG" ]; then
  exit 0
fi
. "$REACT_NATIVE_PATH/scripts/xcode/with-environment.sh"
"$NODE_BINARY" "$PODS_ROOT/../../../../node_modules/expo-modules-autolinking/scripts/ios/replace-xcframework.js" -c "$CONFIG" -m "ExpoModulesWorklets" -x "$PODS_ROOT/ExpoModulesWorklets"

